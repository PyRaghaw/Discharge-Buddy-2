# Mobile App (Discharge Buddy) Configuration

# The local IP address of your machine running the backend
# Use http://localhost:3000 for iOS simulator or web
# Use your machine's local IP (e.g., http://192.168.1.10:3000) for physical Android/iOS devices
EXPO_PUBLIC_API_URL=http://localhost:3000
